version: '3.9'

services:

  # ── Ollama — local LLM ────────────────────────────────────────────────────────
  ollama:
    image: ollama/ollama:latest
    container_name: intelliops-ollama
    volumes:
      - ollama_data:/root/.ollama
    environment:
      - OLLAMA_KEEP_ALIVE=24h
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "ollama list || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 20s

  # ── Ollama model init — pulls qwen2.5:1.5b once on first deploy ───────────────
  ollama-pull:
    image: ollama/ollama:latest
    container_name: intelliops-ollama-pull
    volumes:
      - ollama_data:/root/.ollama
    environment:
      - OLLAMA_HOST=http://ollama:11434
    depends_on:
      ollama:
        condition: service_healthy
    entrypoint: ["ollama", "pull", "qwen2.5:1.5b"]
    restart: "no"

  # ── Monitor API ───────────────────────────────────────────────────────────────
  monitor:
    build:
      context: .
      dockerfile: monitor_server/Dockerfile
    container_name: intelliops-monitor
    environment:
      - AWS_REGION=${AWS_REGION:-ap-south-1}
      - HOME_ACCOUNT_ID=${HOME_ACCOUNT_ID:-036160411876}
      - CROSS_ACCOUNT_ROLE_NAME=${CROSS_ACCOUNT_ROLE_NAME:-CWMSessionRole}
      - OLLAMA_URL=http://ollama:11434
      - OLLAMA_MODEL=${OLLAMA_MODEL:-qwen2.5:1.5b}
      - MONITOR_LAMBDAS=${MONITOR_LAMBDAS:-}
      - MONITOR_DYNAMO_TABLES=${MONITOR_DYNAMO_TABLES:-IntelliOps-Tickets,IntelliOps-Alarms,IntelliOps-Sessions}
      - INTELLIOPS_STRANDS_JOBS_BUCKET=${INTELLIOPS_STRANDS_JOBS_BUCKET:-intelliops-websiterca}
    depends_on:
      - ollama
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8000/health')\""]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 30s

  # ── Multi-Agent API (wmintelliops PoC on port 8001) ──────────────────────────
  multiagent:
    build:
      context: .
      dockerfile: wmintelliops/Dockerfile
    container_name: intelliops-multiagent
    environment:
      - OLLAMA_URL=http://ollama:11434
      - OLLAMA_MODEL=${OLLAMA_MODEL:-qwen2.5:1.5b}
      - MONITOR_URL=http://monitor:8000
      - AWS_REGION=${AWS_REGION:-ap-south-1}
      - HOME_ACCOUNT_ID=${HOME_ACCOUNT_ID:-036160411876}
      - HEALING_DRY_RUN=true
    depends_on:
      - ollama
      - monitor
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8001/health')\""]
      interval: 20s
      timeout: 5s
      retries: 5
      start_period: 30s

  # ── Certbot — Let's Encrypt renewal daemon ────────────────────────────────────
  certbot:
    image: certbot/certbot:latest
    container_name: intelliops-certbot
    volumes:
      - certbot_certs:/etc/letsencrypt
      - certbot_www:/var/www/certbot
    entrypoint: /bin/sh -c 'trap exit TERM; while :; do certbot renew --quiet --webroot -w /var/www/certbot 2>/dev/null; sleep 12h & wait $${!}; done'
    depends_on:
      - nginx
    restart: unless-stopped

  # ── Nginx reverse proxy ───────────────────────────────────────────────────────
  nginx:
    image: nginx:1.27-alpine
    container_name: intelliops-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/init-cert.sh:/docker-entrypoint.d/10-init-cert.sh
      - certbot_certs:/etc/letsencrypt
      - certbot_www:/var/www/certbot
    depends_on:
      monitor:
        condition: service_healthy
    restart: unless-stopped

volumes:
  ollama_data:
  certbot_certs:
  certbot_www:
